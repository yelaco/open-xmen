{
  "name": "open-xmen",
  "version": "0.2.7",
  "description": "Cerebro/X-Men orchestration workflow for OpenCode.",
  "type": "module",
  "bin": {
    "open-xmen": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "dist/index.d.ts",
      "import": "dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "generate-assets": "bun scripts/generate-assets.ts",
    "build": "tsc -p tsconfig.json",
    "doctor": "node dist/cli.js doctor",
    "validate": "npm run build && npm run verify:release",
    "prepare": "npm run build",
    "prepublishOnly": "npm run validate",
    "pack:check": "npm pack --dry-run",
    "verify:release": "bun scripts/verify-release-artifact.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.4.7",
    "zod": "^4.1.8"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "^1.16.2",
    "@types/node": "^22.13.9",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=22"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts"
}
